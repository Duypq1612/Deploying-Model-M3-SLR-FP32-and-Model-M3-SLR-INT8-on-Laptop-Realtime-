# 1. Bỏ qua thư mục chứa các file model nặng
models/

# 2. Bỏ qua thư mục lưu video tải lên (tránh đẩy rác lên GitHub)
api_server/uploads/
uploads/

# 3. Bỏ qua các file rác do Python tự động sinh ra trong quá trình chạy
__pycache__/
*.py[cod]

# 4. Bỏ qua môi trường ảo (nếu bạn có tạo môi trường riêng biệt để cài thư viện)
venv/
env/
.venv/

# 5. Bỏ qua các file ẩn của hệ điều hành
.DS_Store
Thumbs.db